if(
  equals(length(body('Get_items')?['value']),0),
  null,
  int(
    ceiling(
      div(
        float(
          replace(
            replace(
              string(first(body('Get_items')?['value'])?['Master Net Direct Balance']),
              ',',
              ''
            ),
            '$',
            ''
          )
        ),
        1000000
      )
    )
  )
)
