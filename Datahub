if(
  empty(first(body('Get_items')?['value'])?['Master Net Direct Balance']),
  null,
  int(
    ceiling(
      div(
        float(replace(string(first(body('Get_items')?['value'])?['Master Net Direct Balance']),',','')),
        1000000
      )
    )
  )
)
