from openpyxl import Workbook
from openpyxl.styles import Font, PatternFill, Border, Side, Alignment
from openpyxl.utils import get_column_letter

output_file = "tables for Manco.xlsx"

# Colors
RED = "DB0011"
BLACK = "000000"
DARK_GREY = "4A4F55"
LIGHT_GREY = "F4F5F7"
WHITE = "FFFFFF"

# Workbook
wb = Workbook()
wb.remove(wb.active)

# Styles
header_fill = PatternFill("solid", fgColor=DARK_GREY)
section_fill = PatternFill("solid", fgColor=RED)
body_fill = PatternFill("solid", fgColor=WHITE)
alt_fill = PatternFill("solid", fgColor=LIGHT_GREY)

header_font = Font(color=WHITE, bold=True)
section_font = Font(color=WHITE, bold=True)
body_font = Font(size=10)
bold_font = Font(bold=True)

thin = Side(style="thin", color="CCCCCC")
border = Border(left=thin, right=thin, top=thin, bottom=thin)

center = Alignment(horizontal="center", vertical="center", wrap_text=True)
left = Alignment(horizontal="left", vertical="top", wrap_text=True)

def set_width(ws, widths):
    for i, w in widths.items():
        ws.column_dimensions[get_column_letter(i)].width = w

def style(cell, fill=None, font=None, align=None):
    if fill: cell.fill = fill
    if font: cell.font = font
    if align: cell.alignment = align
    cell.border = border

# =====================================================
# SLIDE 16 — SIMPLIFIED (COMMENT → ACTION → FINAL TEXT)
# =====================================================
ws16 = wb.create_sheet("Slide 16")
ws16.sheet_view.showGridLines = False

set_width(ws16, {1: 35, 2: 35, 3: 45})

# Section title
ws16.merge_cells("A1:C1")
cell = ws16["A1"]
cell.value = "Slide 16 – Issues (WACC)"
style(cell, fill=section_fill, font=section_font, align=center)

# Headers
headers = ["Comment from Review", "What you should do", "Final slide wording"]
for i, h in enumerate(headers, 1):
    c = ws16.cell(3, i, h)
    style(c, fill=header_fill, font=header_font, align=center)

rows = [
    [
        "No standardized approach for WACC in EV calculation for non-accrual borrowers with negative equity.",
        "Clearly state this as the core issue.",
        "No standardized approach was identified for WACC calculation in EV assessment of non-accrual borrowers with negative equity."
    ],
    [
        "Example borrowers mentioned.",
        "Include examples to support the issue.",
        "The review referenced YS Garments LLC and WHCG Bidco LLC."
    ],
    [
        "Model WACC does not reflect true financing cost.",
        "Explain why this is a problem.",
        "Model-generated WACC did not accurately reflect the true cost of financing."
    ],
    [
        "Issues with comparables, beta and multiples.",
        "Add as supporting explanation (not main point).",
        "Challenges were also identified in selecting distressed comparables and in variability of beta and valuation multiples."
    ],
    [
        "Recommendation: use 17.5% floor or model WACC.",
        "Present as clear recommendation.",
        "A standardized approach was recommended using the greater of 17.5% or the model-generated WACC."
    ],
    [
        "Final decision remains with SCU.",
        "Add governance clarification at the end.",
        "Final determination remains subject to SCU expert judgment."
    ],
]

start = 4
for r, row in enumerate(rows):
    fill = alt_fill if r % 2 == 0 else body_fill
    for c, val in enumerate(row, 1):
        cell = ws16.cell(start + r, c, val)
        style(cell, fill=fill, font=body_font, align=left)

# =====================================================
# SLIDE 18 — SIMPLIFIED
# =====================================================
ws18 = wb.create_sheet("Slide 18")
ws18.sheet_view.showGridLines = False

set_width(ws18, {1: 35, 2: 35, 3: 45})

ws18.merge_cells("A1:C1")
cell = ws18["A1"]
cell.value = "Slide 18 – Service Feedback"
style(cell, fill=section_fill, font=section_font, align=center)

headers = ["Comment from Review", "What you should do", "Final slide wording"]
for i, h in enumerate(headers, 1):
    c = ws18.cell(3, i, h)
    style(c, fill=header_fill, font=header_font, align=center)

rows = [
    [
        "No matters currently directed at SCU.",
        "State clearly at the beginning.",
        "No matters are currently directed at SCU."
    ],
    [
        "Mention Nebraskaland case.",
        "Add as context only.",
        "Litigation has been initiated by an SCU managed borrower (Nebraskaland) in connection with an alleged fraud on their account."
    ],
    [
        "SCU is not involved.",
        "Clarify explicitly.",
        "SCU is not a party to the proceedings and is not directly involved in the matter."
    ],
    [
        "Combine everything into one final statement.",
        "Use as final slide text box.",
        "No matters are currently directed at SCU. Litigation has been initiated by an SCU managed borrower (Nebraskaland) in connection with an alleged fraud on their account. SCU is not a party to the proceedings and is not directly involved in the matter."
    ],
]

start = 4
for r, row in enumerate(rows):
    fill = alt_fill if r % 2 == 0 else body_fill
    for c, val in enumerate(row, 1):
        cell = ws18.cell(start + r, c,
