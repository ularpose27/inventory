if(
  equals(first(body('Get_items')?['value'])?['field_10'], null),
  null,
  int(
    ceiling(
      div(
        float(first(body('Get_items')?['value'])?['field_10']),
        1000000
      )
    )
  )
)
