# Cell 6 – Read Facility tab

# Read columns F:AU, headers on row 7 (Excel)
facility_raw = pd.read_excel(
    file_path,
    sheet_name="Facility",
    usecols="F:AU",
    header=6        # row 7 in Excel
)

print("facility_raw shape (rows, columns):", facility_raw.shape)

# Create a cleaned copy for Python processing
facility = facility_raw.copy()

# Drop columns that are completely empty (format-only columns)
facility = facility.dropna(axis=1, how="all")

# Drop rows that are completely empty (if any)
facility = facility.dropna(how="all")

print("facility shape after cleaning (rows, columns):", facility.shape)
print(facility.head())
