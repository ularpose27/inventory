# Cell 4 – Read the TOR tab into a dataframe

exchange_rates = pd.read_excel(
    file_path,
    sheet_name="TOR",
    usecols="A:C"
)

print(exchange_rates.head())
print("Rows loaded:", len(exchange_rates))
